/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ["www.springboard.com",
         "miro.medium.com",
    ], // 👈 add the external image domain here
  },
};

export default nextConfig;
